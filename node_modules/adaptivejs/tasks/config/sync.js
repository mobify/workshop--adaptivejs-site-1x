module.exports = function(grunt) {
    return {
        assets: {
            files: [{
                cwd: 'assets',
                src: ['**', '!**/js/ui/**', '!**/styles/**'],
                dest: 'build/'
            }],
            verbose: false
        },
        bower: {
            files: [{
                cwd: 'bower_components',
                src: ['**', '!**/scss/**', '!**/node_modules/**'],
                dest: 'build/bower_components'
            }],
            verbose: false
        }
    };
};
