define(
    {
        load: function (name, req, onload, config) {
            if (config.isBuild) {
                // Ignore this dependency during build
                onload();
            } else {
                // Actually require this module at runtime
                req([name], function (mod) {
                    onload(mod);
                });
            }
        }
    }
);
